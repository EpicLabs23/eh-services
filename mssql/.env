MSSQL_SA_PASSWORD=fHnprX2h6Aj2yev4k1dJ+w==Aa1!
MSSQL_PORT=1433
# Leave as 127.0.0.1 if MSSQL runs on the same host as EHM (the default -
# EHM reaches it via the eh_network bridge IP instead, not this port). If
# MSSQL runs on a separate instance, change to this host's LAN IP (or
# 0.0.0.0) so EHM can reach it over the network - see install-mssql.md.
MSSQL_BIND_ADDRESS=127.0.0.1
